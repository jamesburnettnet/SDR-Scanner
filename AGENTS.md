# Agent Instructions

- Version bumps: see [VERSION.md](VERSION.md) for the current version and
  exactly where it's coded.
- Never commit or push automatically. Every commit and every push requires
  an explicit, separate request from the developer — a prior request to
  commit or push does not carry over to later changes; ask again each time.
